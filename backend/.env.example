# SQLite URL (default file in backend dir)
DATABASE_URL=sqlite:///./lazy_sandbox.db

# Comma-separated origins for browser clients (Vite / CRA)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
